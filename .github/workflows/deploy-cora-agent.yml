name: Deploy Cora Agent

on:
  push:
    branches:
      - main
    paths:
      - "src/app/agents/shopperAgent_initializer.py"
      - "src/prompts/ShopperAgentPrompt.txt"
      - "src/app/agents/agent_initializer.py"
      - "src/app/agents/agent_processor.py"
      - "src/app/tools/aiSearchTools.py"
      - ".github/workflows/deploy-cora-agent.yml"
  workflow_dispatch:

jobs:
  deploy-cora:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create src/.env from secret
        shell: bash
        env:
          ENV: ${{ secrets.ENV }}
        run: |
          echo "$ENV" > src/.env

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install -r src/requirements.txt

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy cora agent
        shell: bash
        run: |
          cd src
          python app/agents/shopperAgent_initializer.py
